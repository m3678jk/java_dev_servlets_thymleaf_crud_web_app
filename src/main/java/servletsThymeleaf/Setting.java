package servletsThymeleaf;

public class Setting {

    // don't use backslash in your path
    // because it's only compatible with Windows system
    // but the forwarslash (/) works with Unix and Windows
    public static final String PATH_TO_TEMPLATES = ("src\\main\\resources\\templates\\");
}
